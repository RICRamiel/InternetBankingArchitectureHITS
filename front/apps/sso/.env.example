# Прокси Vite → BFF (см. корневой .env)
BFF_ORIGIN=http://127.0.0.1:8009

# Публичный origin, с которого открываете SSO в dev (vite HMR / @vite/client).
# Пример: DEV_PUBLIC_ORIGIN=https://auth.example.com
DEV_PUBLIC_ORIGIN=
